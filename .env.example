# Copy to .env and fill in your own keys. Do NOT commit the real .env.

# ---- Required ----
OPENAI_API_KEY='your_openai_api_key_here'
TAVILY_API_KEY='your_tavily_api_key_here'

# ---- Optional LLM providers ----
ANTHROPIC_API_KEY='your_anthropic_api_key_here'
GOOGLE_API_KEY='your_google_api_key_here'

# ---- Optional: evaluation and tracing (LangSmith) ----
LANGSMITH_API_KEY='your_langsmith_api_key_here'
LANGSMITH_TRACING=true
LANGSMITH_PROJECT=mj-personal-project
# Uncomment if you are on the EU/APAC instance:
LANGSMITH_ENDPOINT=https://apac.api.smith.langchain.com

# ---- Optional: Azure OpenAI ----
AZURE_OPENAI_API_KEY='your_azure_openai_api_key_here'
AZURE_OPENAI_ENDPOINT='https://your-resource.services.ai.azure.com/'
AZURE_OPENAI_API_VERSION='2024-02-01'
AZURE_OPENAI_DEPLOYMENT='gpt-4.1'

# ---- Optional: Azure AI Document Intelligence (OCR for scanned PDFs) ----
# Primary text extractor for the PDF chatbot (reads scanned pages MarkItDown
# can't). Leave blank to disable and use MarkItDown only. Free (F0) tier:
# 500 pages/month, 20 calls/minute — the budget below guards it.
AZURE_DOCINTEL_ENDPOINT='https://your-resource.cognitiveservices.azure.com/'
AZURE_DOCINTEL_KEY='your_document_intelligence_key_here'
AZURE_DOCINTEL_MODEL='prebuilt-layout'
AZURE_DOCINTEL_MONTHLY_PAGE_BUDGET='500'

# ---- MongoDB Atlas (users + PDF vector search + gateway chat memory) ----
# Database > Connect > Drivers gives you this string; fill in your DB user's
# password (URL-encode any special characters). MONGODB_DB can be any name.
MONGODB_URI='mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=<cluster>'
MONGODB_DB='superbot'

# ---- Auth ----
# JWT signing secret for SuperBot login. Generate a fresh one per environment:
#   python -c "import secrets; print(secrets.token_hex(32))"
AUTH_SECRET='generate_a_64_char_hex_secret_here'
