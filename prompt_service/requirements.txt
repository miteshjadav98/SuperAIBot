fastapi>=0.115
uvicorn[standard]>=0.30
motor>=3.6
pydantic>=2.8
pydantic-settings>=2.4
redis>=5.0            # optional at runtime; only used when REDIS_URL is set

# tests
pytest>=8.0
pytest-asyncio>=0.24
